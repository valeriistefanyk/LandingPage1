nav.nav
                - var item = ['Home', 'About Us', 'Contacts']
                each val in item
                    if val === 'Home'
                        a.nav__link.nav__link--active(href="#")=val
                    else
                        a.nav__link(href="#")=val